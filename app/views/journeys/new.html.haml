- flash.each do |name, msg|
  = content_tag :div, msg, class: "flash-alert"

%h1 Create a new journey

= form_for :journey, url: {action: "create"} do |f|
  = f.label :title
  = f.text_field :title
  %p
  = f.label :description
  = f.text_field :description
  %p
  = f.submit "Create Journey"